<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>com.pan.dashstyle</id>

    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-name -->
    <name>DashStyle CSS Module Support</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor url="https://github.com/robertpanvip/dashstyle">pan</vendor>
    <!-- i18n：plugin.xml 中的 %key% 从该 bundle 解析；代码侧经 DashStyleBundle（DynamicBundle）按 IDE 语言动态解析 -->
    <resource-bundle>messages.DashStyleBundle</resource-bundle>
    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-description -->
    <description><![CDATA[
<p>Enhances IntelliJ IDEA / WebStorm with richer support for CSS Modules (React, Vue, or any project using <code>styles["foo-bar"]</code> / <code>styles.fooBar</code>).</p>

<p>Key features:</p>
<ul>
  <li><strong>Smart completion &amp; navigation</strong>: auto-complete class names for both <code>styles["foo-bar"]</code> (string key) and <code>styles.fooBar</code> (member access); Ctrl+Click jumps to the matching <code>.foo-bar</code> rule, and hovering shows the expanded full rule with its declarations.</li>
  <li><strong>Inline style → CSS Module extraction</strong> (Alt+Enter): turns <code>style={{...}}</code> / <code>:style="{...}"</code> into a semantic class name and appends the rule to the matching module file.</li>
  <li><strong>JSON → CSS copy-paste</strong>: automatically fixes JS style objects pasted into CSS (unitless properties, negatives, transform functions, shorthand arrays); recognizes Vue <code>:style</code> / <code>v-bind:style</code> and Angular <code>[style]</code> / <code>[ngStyle]</code> bindings, including ngStyle unit suffixes such as <code>'font-size.px'</code>.</li>
  <li><strong>Code inspection</strong>: marks unused CSS Module classes and duplicate declaration blocks, with one-click fixes to delete or extract shared styles.</li>
  <li><strong>Tailwind support</strong>: completion with a CSS preview inside <code>@apply</code>, and auto-generation of missing atomic classes referenced via <code>styles.xxx</code>.</li>
  <li><strong>Batch migration</strong>: Refactor → Convert className to CSS Module moves a file or selection onto CSS Modules in one go.</li>
</ul>

<p>Works with CSS / SCSS / LESS module files, Vue <code>&lt;style module&gt;</code>, and local JS/TS object literals.</p>
]]></description>

    <change-notes>
        <![CDATA[
        <h3>1.3.2</h3>
        <ul>
            <li>✨ JSON → CSS 复制粘贴支持 Vue / Angular 绑定前缀：整段复制 <code>:style="{...}"</code> / <code>v-bind:style="{...}"</code> / <code>[style]="{...}"</code> / <code>[ngStyle]="{...}"</code> 粘贴到 CSS / SCSS / LESS（或 .vue 的 <code>&lt;style&gt;</code> 块）时同样自动转换</li>
            <li>✨ 支持 Angular ngStyle 键单位修饰：<code>'font-size.px': 12</code> → <code>font-size: 12px</code>、<code>'width.%': 50</code> → <code>width: 50%</code>（显式单位优先于默认推断，shorthand 数组同样生效）</li>
            <li>🧪 测试加固：inspection 注册路径（EP 实例化逐语言触发）、CreateMissingCssClass 意图菜单、批量迁移类名收集、Tailwind 补全格式化、悬浮文档 CSS Module 主路径等新增常驻测试</li>
        </ul>

        <h3>1.3.1</h3>
        <ul>
            <li>🔧 修复 <code>CreateMissingCssClassIntention</code>（缺失类快速创建）在 Intention 预览触发下可能出现的死锁：通过声明 <code>startInWriteAction = true</code>，让 <code>invoke()</code> 由框架在写锁内调用，避免 preview 的读锁与写锁竞争导致 UI 卡死</li>
            <li>📝 同步插件市场描述、README、FEATURES 说明到最新功能状态（补齐 member access、Inline 抽取、批量迁移等）</li>
        </ul>

        <h3>1.3.0</h3>
        <ul>
            <li>🗑️ 移除 Flex/Grid 布局可视化预览（gutter LineMarker + 交互弹窗）及对应测试</li>
            <li>🗑️ 移除阴影预览（box-shadow / text-shadow gutter 预览）及对应测试</li>
            <li>🗑️ 移除全局 HighlightVisitor（StaticGlobalHighlightVisitor），避免干扰 TypeScript/JavaScript 高亮</li>
            <li>🔧 Java target 升级至 21；修复选择器展开缓存的失效范围，外部修改文件后不再被覆盖</li>
        </ul>

        <h3>1.2.7</h3>
        <ul>
            <li>🗑️ 清理曾规划但未对外暴露的编辑自动化能力，核心功能（<code>styles.xxx</code> / <code>styles["xxx"]</code> 智能跳转/补全、Inline Style 抽取、JSON→CSS 复制粘贴、代码检查、Tailwind 补全、缺失类快速创建、重复声明抽取）均保留：</li>
            <ul>
                <li>移除 CSS Module 导入自动化（含复制时自动带 import）</li>
                <li>移除 TSX/Vue 复制 → CSS 规则随动</li>
                <li>移除 SCSS / LESS / 原生 CSS Nesting 互转（Intention Action）</li>
                <li>移除项目公共颜色 → CSS Variable 抽取（AnAction）</li>
                <li>移除 CSS 尺寸/单位换算助手（Inlay）</li>
            </ul>
        </ul>

        <h3>1.2.0</h3>
        <ul>
            <li>✨ 新增 <code>styles.fooBar</code> member access 跳转/补全（此前仅支持 <code>styles["foo-bar"]</code>）</li>
            <li>✨ 鼠标悬浮 CSS Module class，显示展开后的完整规则声明（含 kebab 选择器、声明列表、来源位置）</li>
            <li>✨ 未使用的 CSS Module class 自动置灰，Alt+Enter 可直接删除（遇动态 <code>styles[expr]</code> 自动关闭，不误报）</li>
            <li>✨ 缺失 class 快速创建：<code>styles.xxx</code> 解析失败时 Alt+Enter，在对应 CSS Module 末尾生成空 class 并把光标定位到大括号内</li>
            <li>✨ 单文件重复 CSS 声明检测（变黄 + 波浪线），一键抽取公共类并生成 <code>@extend</code> 回所有重复点（SCSS/LESS）</li>
        </ul>

        <h3>1.0.8</h3>
        <ul>
            <li>✨ 支持 LESS / SCSS 嵌套规则（nested rulesets）</li>
            <li>✨ 支持 &-xxx / &__xxx 选择器解析</li>
            <li>🐛 修复嵌套 class 无法跳转的问题</li>
        </ul>

        <h3>1.1.0</h3>
        <ul>
            <li>初始版本</li>
        </ul>
        ]]>
    </change-notes>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.xml</depends>
    <!-- CSS 插件（WebStorm / IDEA Ultimate / PyCharm Pro 内置）。
         历史上的 "com.intellij.modules.css-openapi" 已在 2023.3+ 被拆分废弃，
         2025.3 现代版本的 bundled plugin ID 统一是 "com.intellij.css"，
         否则本地安装会弹「需要安装 com.intellij.modules.css-openapi 模块」的提示。-->
    <depends>com.intellij.css</depends>
    <depends>JavaScript</depends>
    <depends optional="true">org.jetbrains.plugins.vue</depends>
    <!-- Angular（发行版插件 ID 为 "AngularJS"，目录 plugins/angular）。
         optional：未装 Angular 插件的 WebStorm 上 DashStyle 仍可安装；
         装了则可利用其模板/组件 PSI 能力做更精确的框架识别。-->
    <depends optional="true">AngularJS</depends>

    <!-- Extension points defined by the plugin.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
    <extensions defaultExtensionNs="com.intellij">
        <!-- ========================= Tailwind 类补全（@apply 内，右侧 CSS 预览） ========================= -->
        <!-- 在 CSS 的 @apply 指令内对 Tailwind 类做补全，候选右侧灰字显示该类展开后的 CSS 声明，
             按 Enter 直接补全。类清单来自 TailwindClassResolver（纯逻辑，内置常用类）。 -->
        <completion.contributor
                language="CSS"
                implementationClass="com.pan.dashstyle.reference.TailwindClassCompletionContributor"/>
        <completion.contributor
                language="SCSS"
                implementationClass="com.pan.dashstyle.reference.TailwindClassCompletionContributor"/>
        <completion.contributor
                language="LESS"
                implementationClass="com.pan.dashstyle.reference.TailwindClassCompletionContributor"/>

        <!-- ========================= 引用 & 跳转 ========================= -->
        <psi.referenceContributor
                implementation="com.pan.dashstyle.reference.StyleStringKeyReferenceContributor"/>

        <!-- styles.xxx member access 也能跳转/补全 -->
        <psi.referenceContributor
                implementation="com.pan.dashstyle.reference.StyleMemberAccessReferenceContributor"/>

        <!-- ========================= 悬浮文档（#4） ========================= -->
        <lang.documentationProvider
                language="CSS"
                implementationClass="com.pan.dashstyle.annotator.DashStyleDocumentationProvider"
                order="first"/>
        <lang.documentationProvider
                language="JavaScript"
                implementationClass="com.pan.dashstyle.annotator.DashStyleDocumentationProvider"
                order="first"/>
        <lang.documentationProvider
                language="TypeScript"
                implementationClass="com.pan.dashstyle.annotator.DashStyleDocumentationProvider"
                order="first"/>
        <lang.documentationProvider
                language="TypeScript JSX"
                implementationClass="com.pan.dashstyle.annotator.DashStyleDocumentationProvider"
                order="first"/>
        <lang.documentationProvider
                language="SCSS"
                implementationClass="com.pan.dashstyle.annotator.DashStyleDocumentationProvider"
                order="first"/>
        <lang.documentationProvider
                language="LESS"
                implementationClass="com.pan.dashstyle.annotator.DashStyleDocumentationProvider"
                order="first"/>

        <!-- ========================= 代码检查（#1 未使用 class / #2 重复 declarations） ========================= -->
        <!-- 注意：<localInspection> 的 shortName 在整个插件里必须唯一（哪怕 implementationClass / language 不同）。
             下面按语言维度分别赋予唯一 shortName；Kotlin 侧 Inspection 类不再 override getShortName()，
             直接继承 LocalInspectionTool 默认返回 XML 声明的 shortName，避免代码与 XML 漂移。
             历史上曾因 SCSS/LESS 的官方 language id 在不同发行版里 id 不匹配（如 SCSS 插件没启用、LESS id 变成 Less）
             额外加了 language="ANY" 兜底注册，但经沙箱探针实证：localInspection EP 对这两个类按 language 精确分发，
             .css/.scss/.less 会各自触发对应注册，ANY 从未被实际用到——故已删除（8 → 6 注册）并新增常驻测试防回归
             （见 DashStyleIntegrationTest 中 "pluginDescriptor localInspection registrations are complete and ANY fallbacks removed"）。
             具体要不要处理，Kotlin 代码里会通过 CssRuleset / file suffix 判断，不影响其他语言。-->
        <localInspection
                language="CSS"
                displayName="%inspection.unused.css.module.class.display.name%"
                groupName="DashStyle"
                shortName="DashStyle.UnusedCssClass.Css"
                enabledByDefault="true"
                level="WEAK WARNING"
                implementationClass="com.pan.dashstyle.inspection.UnusedCssModuleClassInspection"/>
        <localInspection
                language="SCSS"
                displayName="%inspection.unused.css.module.class.display.name%"
                groupName="DashStyle"
                shortName="DashStyle.UnusedCssClass.Scss"
                enabledByDefault="true"
                level="WEAK WARNING"
                implementationClass="com.pan.dashstyle.inspection.UnusedCssModuleClassInspection"/>
        <localInspection
                language="LESS"
                displayName="%inspection.unused.css.module.class.display.name%"
                groupName="DashStyle"
                shortName="DashStyle.UnusedCssClass.Less"
                enabledByDefault="true"
                level="WEAK WARNING"
                implementationClass="com.pan.dashstyle.inspection.UnusedCssModuleClassInspection"/>
        <localInspection
                language="CSS"
                displayName="%inspection.duplicate.css.declarations.display.name%"
                groupName="DashStyle"
                shortName="DashStyle.DuplicateCss.Css"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.pan.dashstyle.inspection.DuplicateCssDeclarationsInspection"/>
        <localInspection
                language="SCSS"
                displayName="%inspection.duplicate.css.declarations.display.name%"
                groupName="DashStyle"
                shortName="DashStyle.DuplicateCss.Scss"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.pan.dashstyle.inspection.DuplicateCssDeclarationsInspection"/>
        <localInspection
                language="LESS"
                displayName="%inspection.duplicate.css.declarations.display.name%"
                groupName="DashStyle"
                shortName="DashStyle.DuplicateCss.Less"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="com.pan.dashstyle.inspection.DuplicateCssDeclarationsInspection"/>

        <annotator language="CSS" implementationClass="com.pan.dashstyle.annotator.DashStyleHighlightAnnotator"/>
        <annotator language="SCSS" implementationClass="com.pan.dashstyle.annotator.DashStyleHighlightAnnotator"/>
        <annotator language="LESS" implementationClass="com.pan.dashstyle.annotator.DashStyleHighlightAnnotator"/>

        <!-- ========================= 复制粘贴处理器 ========================= -->
        <!-- 用户反馈：复制 `styles.xxx` → 粘贴到别处的场景，IntelliJ 平台自带「Add import on paste」已能自动补 import，
             所以移除 DashStyle 自己实现的 CssModuleImportCopyPasteProcessor（#3）避免两条逻辑冲突。-->
        <copyPastePreProcessor
                implementation="com.pan.dashstyle.action.JsonToCssCopyPastePreProcessor"/>

        <!-- ========================= 快速修复 / 意向动作 ========================= -->
        <!-- 重要：这里故意不写 <language> 绑定。
             原因：WebStorm 2025.3 对 intention 的语言匹配存在方言(dialect)继承问题——
             即使诊断确认了正确的 language ID（"TypeScript JSX" 等），实际运行时仍会因
             平台内部 dialect/baseLanguage 链的匹配逻辑跳过 intention。
             因此采用"全局注册 + isAvailable() 运行时精确过滤"策略：
             isAvailable() 内部已经通过 JSReferenceExpression / JSIndexedPropertyAccessExpression /
             XmlAttribute / CssRuleset 等 PSI 类型做了严格的上下文检查，不会在不相关语言里误触发。-->
        <intentionAction>
            <className>com.pan.dashstyle.action.InlineStyleToCssModuleIntention</className>
            <category>DashStyle</category>
            <description><![CDATA[
              Extracts the inline style object (<code>style={{ ... }}</code> in React/TSX
              or <code>:style=&quot;{ ... }&quot;</code> in Vue) into a CSS Module class.<br/>
              <ul>
                <li>Automatic semantic class name inference (from component name, siblings, and style property semantics).</li>
                <li>Rename-style input dialog lets you tweak the name before committing.</li>
                <li>Appends the rule to <code>&lt;style module&gt;</code> (.vue) or the imported
                    <code>*.module.css</code>/<code>.scss</code>/<code>.less</code> file.</li>
              </ul>
            ]]></description>
            <bindToCode>true</bindToCode>
        </intentionAction>

        <intentionAction>
            <className>com.pan.dashstyle.action.CreateMissingCssClassIntention</className>
            <category>DashStyle</category>
            <description><![CDATA[
              When <code>styles.fooBar</code> or <code>styles["foo-bar"]</code> has no corresponding CSS class,
              creates <code>.foo-bar { }</code> at the end of the target CSS Module (or Vue
              <code>&lt;style module&gt;</code>) and places the caret inside the braces. If the missing
              name matches a built-in Tailwind utility class, the expanded CSS declaration is generated
              instead of an empty block (e.g. <code>.flex { display: flex }</code>).
            ]]></description>
            <bindToCode>true</bindToCode>
        </intentionAction>

        <intentionAction>
            <className>com.pan.dashstyle.action.ExtractDuplicateDeclarationsAsMixinIntention</className>
            <category>DashStyle</category>
            <description><![CDATA[
              Scans the current CSS/SCSS/LESS file (or the selected portion) for
              rulesets sharing exactly the same declaration block body and
              factors them out into a single shared Less-style mixin (e.g.
              <code>.shared-padding { padding: 10px; }</code>), replacing each
              original ruleset's body with a mixin call (e.g.
              <code>.shared-padding;</code>). Nested blocks, strings and
              function calls like <code>linear-gradient(...)</code> /
              <code>url("...")</code> are preserved verbatim during comparison.
            ]]></description>
            <bindToCode>true</bindToCode>
        </intentionAction>

        <!-- postStartupActivity 诊断块已移除（DashStyleLanguageDumpActivity 用完即删） -->
    </extensions>

    <actions>
        <action id="DashStyle.ConvertClassNameToCssModule"
                class="com.pan.dashstyle.action.ConvertClassNameToCssModuleAction"
                text="%action.DashStyle.ConvertClassNameToCssModule.text%"
                description="%action.DashStyle.ConvertClassNameToCssModule.description%">
            <add-to-group group-id="RefactoringMenu" anchor="last"/>
            <!-- 编辑器右键直接可见，方便 TSX/JSX 下快速触发。
                 注意：不要引用 "RefactoringPopup" group——该 group id 在 WebStorm 平台未注册，
                 引用未注册的 group 会在插件加载时抛 ActionManager 断言错误（连累本地集成测试）。 -->
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
